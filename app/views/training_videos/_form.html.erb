<%= form_with(model: training_video, local: true) do |form| %>
  <% if training_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(training_video.errors.count, "error") %> prohibited this training_video from being saved:</h2>

      <ul>
      <% training_video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
