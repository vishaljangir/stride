<%= form_with(model: news_latest, local: true) do |form| %>
  <% if news_latest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news_latest.errors.count, "error") %> prohibited this news_latest from being saved:</h2>

      <ul>
      <% news_latest.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
